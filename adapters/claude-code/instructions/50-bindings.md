## Claude Code bindings

Concrete surfaces for rules stated neutrally above.

- Reply language pinned by the runtime: the `language` field in `settings.json`.
- Verifying a tool is available before relying on it: `ToolSearch`.
- Permission rules that enforce credential handling mechanically: the deny patterns in `settings.json`.

