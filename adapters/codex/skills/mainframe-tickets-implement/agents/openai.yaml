interface:
  display_name: "MAINFRAME Ticket Implementation"
  short_description: "Implement and validate ready tickets"
  default_prompt: "Use $mainframe-tickets-implement to implement, validate, and queue every technically ready ticket for independent verification."
policy:
  allow_implicit_invocation: false
