interface:
  display_name: "MAINFRAME Pi Engineer"
  short_description: "Run one bounded implementation block"
  default_prompt: "Use $mainframe-pi-engineer to execute one already-agreed implementation block in the current worktree."
policy:
  allow_implicit_invocation: true
