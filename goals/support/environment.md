# Establish the current environment

Identify the hosting product, interface, active workspace, and requested
operation from the supplied context and relevant native evidence. State them
briefly before inspecting configuration.

Use that identity to select current official documentation for each layer.
Establish shared Desktop/CLI paths and capabilities from their actual contracts.
Keep model, CLI, and Desktop versions distinct; record unknown versions without
blocking independent inspection. Correct the scope if new evidence changes it.
